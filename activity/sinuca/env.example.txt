# O frontend é um site puro e usa a API no mesmo domínio (/api).
# Nenhum client secret, token do Discord ou URL de Activity deve ser exposto pelo Vite.

# Arte transparente opcional da página inicial (PNG, WebP, GIF ou SVG; caminho ou URL).
VITE_DASHBOARD_DECORATIVE_IMAGE_URL=
VITE_DASHBOARD_DECORATIVE_IMAGE_SIZE=clamp(16rem, 31vw, 29rem)
VITE_DASHBOARD_DECORATIVE_IMAGE_TOP=clamp(6rem, 10vw, 9rem)
VITE_DASHBOARD_DECORATIVE_IMAGE_RIGHT=clamp(1rem, 7vw, 7rem)
VITE_DASHBOARD_DECORATIVE_IMAGE_OPACITY=0.9

# GIF transparente opcional dos carregamentos principais. O spinner continua como fallback.
VITE_DASHBOARD_LOADING_GIF_URL=
VITE_DASHBOARD_LOADING_GIF_SIZE=60px
